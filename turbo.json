{
	"$schema": "https://turbo.build/schema.json",
	"ui": "tui",
	"concurrency": "100%",
	"tasks": {
		"dev": {
			"persistent": true,
			"cache": false
		},
		"build": {
			"dependsOn": ["^build"],
			"outputs": ["dist/**", ".vercel/**"]
		}
	}
}
